# Database
GP_DB_HOST=127.0.0.1
GP_DB_PORT=5432
GP_DB_NAME=greenpulse
GP_DB_USER=greenpulse_app
GP_DB_PASS=CHANGE_ME_STRONG_DB_PASSWORD

# JWT
GP_JWT_SECRET=CHANGE_ME_LONG_RANDOM_SECRET
GP_JWT_ISSUER=greenpulse
GP_JWT_AUDIENCE=greenpulse
GP_JWT_TTL=86400

# App security
GP_DEBUG=false
GP_CORS_ORIGIN=https://your-domain.tld
